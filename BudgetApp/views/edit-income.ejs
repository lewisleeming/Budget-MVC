<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Income - BudgetApp</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header class="app-header">
        <div class="header-container">
            <a href="/dashboard" class="brand-link">💰 BudgetApp</a>
            <div class="header-actions">
                <a href="/dashboard" class="btn btn-secondary btn-sm">← Back to Dashboard</a>
            </div>
        </div>
    </header>

    <main class="page-container">
        <div class="form-card">
            <div class="card-header">
                <h2>Edit Income</h2>
                <p class="text-muted">Modify income transaction details</p>
            </div>

            <% if (messages.error && messages.error.length > 0) { %>
                <div class="alert alert-error" role="alert">
                    <% messages.error.forEach(function(error) { %>
                        <p><%= error %></p>
                    <% }) %>
                </div>
            <% } %>

            <form action="/incomes/<%= income._id %>/edit" method="POST" class="standard-form">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                <div class="form-group">
                    <label for="amount">Amount (£) <span class="required">*</span></label>
                    <input 
                        type="number" 
                        id="amount" 
                        name="amount" 
                        value="<%= income.amount %>" 
                        step="0.01" 
                        min="0.01" 
                        max="10000000"
                        required 
                        autofocus
                    >
                </div>

                <div class="form-group">
                    <label for="description">Description <span class="required">*</span></label>
                    <input 
                        type="text" 
                        id="description" 
                        name="description" 
                        value="<%= income.description %>" 
                        maxlength="200" 
                        required
                    >
                </div>

                <div class="form-group">
                    <label for="category">Category <span class="required">*</span></label>
                    <select id="category" name="category" required>
                        <% categories.forEach(function(cat) { %>
                            <option value="<%= cat %>" <%= (income.category === cat) ? 'selected' : '' %>><%= cat %></option>
                        <% }) %>
                    </select>
                </div>

                <div class="form-group">
                    <label for="date">Transaction Date <span class="required">*</span></label>
                    <input type="date" id="date" name="date" value="<%= formattedDate %>" required>
                </div>

                <div class="form-group checkbox-group">
                    <label class="checkbox-label">
                        <input type="checkbox" id="isRecurring" name="isRecurring" value="true" <%= income.isRecurring ? 'checked' : '' %> onchange="toggleRecurringOptions(this)">
                        <span>This is recurring income</span>
                    </label>
                </div>

                <div class="form-group" id="frequency-group" style="<%= income.isRecurring ? '' : 'display: none;' %>">
                    <label for="frequency">Frequency</label>
                    <select id="frequency" name="frequency">
                        <option value="monthly" <%= income.frequency === 'monthly' ? 'selected' : '' %>>Monthly</option>
                        <option value="weekly" <%= income.frequency === 'weekly' ? 'selected' : '' %>>Weekly</option>
                        <option value="yearly" <%= income.frequency === 'yearly' ? 'selected' : '' %>>Yearly</option>
                    </select>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <a href="/dashboard" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </main>

    <script src="/js/app.js"></script>
</body>
</html>
